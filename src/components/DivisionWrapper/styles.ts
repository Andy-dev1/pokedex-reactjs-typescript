import styled from "styled-components";
import {Col as ColBoostrap} from 'react-bootstrap';

export const Col = styled(ColBoostrap)`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
`;
